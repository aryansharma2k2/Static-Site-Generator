# 📝 Static Site Generator (Markdown → HTML)

A lightweight **static website generator** written in **Python** that converts Markdown files into clean, responsive HTML pages styled with CSS. The generated sites are easily deployable to **GitHub Pages**, making content publishing simple, fast, and version-controlled.

## 🚀 Features

* 📄 Write content using **Markdown**
* 🔄 Automatically converts Markdown to **HTML**
* 🎨 Styled with **CSS** for a clean, responsive layout
* 🧩 Simple templating system for reusable layouts
* ⚡ Fast static site generation (no database required)
* 🌍 Seamless deployment to **GitHub Pages**
* 📦 Designed for **simplicity and scalability**

## 🛠️ Tech Stack

* **Language:** Python
* **Markup:** Markdown
* **Output:** HTML + CSS
* **Deployment:** GitHub Pages

## ⚙️ How It Works

1. Users write content in Markdown files
2. The generator parses Markdown into HTML
3. HTML content is injected into reusable templates
4. CSS is applied for consistent styling
5. Final static files are generated and deployed

## ▶️ Getting Started

### Prerequisites

* Python 3.x
* Git

### Installation

```bash
git clone https://github.com/aryansharma2k2/static_site_generator.git
cd static_site_generator
```

### Generate the Site

Simply run the main.sh script.

The generated HTML files will appear in the `output/` directory.

## 🌐 Deployment

The generated site can be deployed to **GitHub Pages** by pushing the output directory to a GitHub repository configured for Pages.

## 📌 Use Cases

* Personal blogs
* Documentation websites
* Portfolio sites
* Lightweight content-driven websites

## 🎯 Learning Outcomes

This project demonstrates:

* Backend development with Python
* Markdown parsing
* HTML templating
* Static site generation principles
* GitHub Pages deployment workflow

## 📄 License

This project is open source and available under the **MIT License**.
